{
  "name": "claude-observe",
  "version": "0.7.0",
  "description": "Real-time observability dashboard for Claude Code agents",
  "license": "MIT",
  "repository": "https://github.com/simple10/claude-observe",
  "homepage": "https://github.com/simple10/claude-observe",
  "private": true,
  "scripts": {
    "start": "node start.mjs",
    "test": "vitest run && cd app/server && npm test && cd ../client && npm test",
    "test:server:watch": "cd app/server && npm run test:watch",
    "test:client:watch": "cd app/server && npm run test:watch"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
