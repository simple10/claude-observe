// Re-export from the claude-code agent module.
// Kept for backwards compatibility with icon-settings and timeline-rewind.
export {
  eventIcons,
  eventColors,
  defaultEventIcon,
  resolveEventKey,
  getEventColor,
  getEventIcon,
} from '@/agents/claude-code/icons'
